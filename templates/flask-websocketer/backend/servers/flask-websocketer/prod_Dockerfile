FROM $(baseImage)
MAINTAINER $(maintainer)

ADD . /opt/docker/

ENV DOCKER_HOST $(host)
ENV SITE_NAME $(siteName)
ENV TYPE prod
ENV WEB on
$(extraConfig)

EXPOSE $(prodPort)

RUN pip install -r config/websocketer_requirements.txt

CMD gunicorn --config config/prod_guwsgi.ini app
