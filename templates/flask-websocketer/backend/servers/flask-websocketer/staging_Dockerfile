FROM $(baseImage)
MAINTAINER $(maintainer)

ADD . /opt/docker/

ENV DOCKER_HOST $(host)
ENV SITE_NAME $(siteName)
ENV TYPE staging
ENV WEB on
$(extraConfig)

EXPOSE $(port)

RUN pip install -r config/requirements.txt

CMD gunicorn --config config/staging_guwsgi.ini bapp
